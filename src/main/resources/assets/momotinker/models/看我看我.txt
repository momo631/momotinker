这部分是存放物品模型的

这个模型是指将贴图等等东西定位到物品实例的东西

注意路径不要写错，文件名要和注册名一致

item是物品类
block是方块类

值得注意的是方块需要一共三个model相关的json
一个是拿手里的，一个是外显，一个是放置的

另外，桶流体只需要model不需要材质，因为可以自动生成，但是要model

分别对应item，block和assets下面那个blockstates里面
